# Electron 本体二进制（约 285MB）默认从 GitHub releases 下载，国内网络经常失败，
# 报错 "Electron failed to install correctly"。这里把下载源改成国内镜像（阿里云 npmmirror）。
# 注意：仅影响 electron 二进制的下载，不改变 npm 包源；海外用户同样可访问该镜像。
# 如需走默认源或自定义镜像，删除/修改本行即可，或用环境变量 ELECTRON_MIRROR 覆盖。
electron_mirror=https://npmmirror.com/mirrors/electron/
